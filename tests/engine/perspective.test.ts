import assert from "node:assert/strict";
import test from "node:test";
import { resolveTeamPerspectives, selectPerspective, type LeagueSnapshot } from "../../src/engine/index";
const snapshot=(marvinTeam="B"):LeagueSnapshot=>({id:"L",season:2026,name:"L",currentWeek:1,primaryTeamId:"A",dataMode:"live",players:{"4432708":{id:"4432708",name:"Marvin Harrison Jr.",nflTeam:"ARI",primaryPosition:"WR",eligiblePositions:["WR"],rosteredTeamId:marvinTeam,forecasts:[],provenance:{source:"t",asOf:"x",retrievedAt:"x"},externalIds:{espn:"4432708"}}},freeAgentIds:[],waiverPlayerIds:[],schedule:[],settings:{scoring:[],lineupSlots:[],rosterSize:1,positionLimits:{},irSlots:0,playoffWeeks:[],raw:{}},teams:[{id:"A",name:"Mine",ownerIds:[],record:{wins:0,losses:0,ties:0},roster:[]},{id:"B",name:"Marvin owner",ownerIds:[],record:{wins:0,losses:0,ties:0},roster:[{playerId:"4432708",assignedSlotId:20,location:"bench"}]}],provenance:{source:"t",asOf:"x",retrievedAt:"x"}});
test("perspective resolver exposes exactly My Team and Marvin Team from current Marvin ownership",()=>{const options=resolveTeamPerspectives(snapshot());assert.deepEqual(options.map(x=>x.perspective),["MY_TEAM","MARVIN_TEAM"]);assert.equal(options[1].teamId,"B");assert.equal(selectPerspective(snapshot(),"MARVIN_TEAM").primaryTeamId,"B");});
test("Marvin missing fails closed and a fresh owner resolves again",()=>{const missing=snapshot(); delete missing.players["4432708"]; assert.equal(resolveTeamPerspectives(missing)[1].available,false);assert.equal(selectPerspective(snapshot("A"),"MARVIN_TEAM").primaryTeamId,"A");});
